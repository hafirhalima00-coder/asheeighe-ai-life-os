import 'package:flutter/material.dart';

abstract final class AsheeigheColors {
  AsheeigheColors._();

  // ──────────────────────────────────────────────
  // Primary Pastel Palette
  // ──────────────────────────────────────────────
  static const Color pastelPink = Color(0xFFF4C2C2);
  static const Color softLavender = Color(0xFFE6E6FA);
  static const Color creamWhite = Color(0xFFFFF8F0);
  static const Color warmBeige = Color(0xFFF5E6D3);
  static const Color lightPeach = Color(0xFFFFDAB9);
  static const Color softRose = Color(0xFFFFB6C1);
  static const Color gentlePurple = Color(0xFFD8BFD8);

  // ──────────────────────────────────────────────
  // Accent Colors
  // ──────────────────────────────────────────────
  static const Color mint = Color(0xFF98FB98);
  static const Color skyBlue = Color(0xFF87CEEB);
  static const Color gold = Color(0xFFFFD700);

  // ──────────────────────────────────────────────
  // Dark Mode Variants
  // ──────────────────────────────────────────────
  static const Color darkPastelPink = Color(0xFFC29292);
  static const Color darkSoftLavender = Color(0xFFB6B6D4);
  static const Color darkWarmBeige = Color(0xFFC5B6A3);
  static const Color darkLightPeach = Color(0xFFCCAA89);
  static const Color darkSoftRose = Color(0xFFCF8694);
  static const Color darkGentlePurple = Color(0xFFA88FA8);
  static const Color darkMint = Color(0xFF68CB68);
  static const Color darkSkyBlue = Color(0xFF57AECE);

  // ──────────────────────────────────────────────
  // Surface Colors – Light
  // ──────────────────────────────────────────────
  static const Color surfaceLight = Color(0xFFFFF8F0);
  static const Color surfaceContainerLight = Color(0xFFFFF0E0);
  static const Color surfaceOnLight = Color(0xFF1C1B1F);

  // ──────────────────────────────────────────────
  // Surface Colors – Dark
  // ──────────────────────────────────────────────
  static const Color surfaceDark = Color(0xFF1C1B1F);
  static const Color surfaceContainerDark = Color(0xFF2B2930);
  static const Color surfaceOnDark = Color(0xFFE6E1E5);

  // ──────────────────────────────────────────────
  // Semantic Colors
  // ──────────────────────────────────────────────
  static const Color success = Color(0xFF4CAF50);
  static const Color successContainer = Color(0xFFC8E6C9);
  static const Color warning = Color(0xFFFFC107);
  static const Color warningContainer = Color(0xFFFFF9C4);
  static const Color error = Color(0xFFE53935);
  static const Color errorContainer = Color(0xFFFFCDD2);
  static const Color info = Color(0xFF2196F3);
  static const Color infoContainer = Color(0xFFBBDEFB);

  // ──────────────────────────────────────────────
  // Glass / Frost
  // ──────────────────────────────────────────────
  static const Color glassLight = Color(0xCCFFF8F0);
  static const Color glassDark = Color(0xCC1C1B1F);
  static const Color glassStrokeLight = Color(0x1A000000);
  static const Color glassStrokeDark = Color(0x1AFFFFFF);
}
